/ {
  behaviors {
    // NOTE: This is not working yet due to locality
    // reset_td: reset_td {
    //   compatible = "zmk,behavior-tap-dance";
    //   #binding-cells = <0>;
    //   tapping-term-ms = <200>;
    //   bindings = <&sys_reset>, <&bootloader>;
    // };
  };
};
